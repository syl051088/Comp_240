CC = gcc

hello: hello.c
	$(CC) -o hello hello.c

clean:
	rm hello

#Notes:
#   lines 4 and 7 are indented with a tab (for make, it MUST be a tab)
#   $(CC) is replaced with what is to the right of the = in line 1
#   comments like this begin with #
